// DIE Scheduler scaffold
// Future owner of adaptive refresh timing based on next-day MLB schedule and first pitch times.
export const Scheduler = {
  version: '8.1',
  enabled: false,
  initialize() { return true; }
};
